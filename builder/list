#!/bin/bash

while read line; do
	line=${line// /\"\ \"}
	while read file; do
		echo $file
	done < <(eval ls -1 "${line}" 2> /dev/null)  
		


done < <(sed test.sh -e '/^function includes/,/^}/p;d' | sed '1d;$d')
