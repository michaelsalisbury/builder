#!/bin/bash

while read line; do
	#echo $line
	#line=${line// /\"\ \"}
	line=${line// /\" \"}
	
	#for each in $(echo "${line}"); do
	#for each in $(eval echo "${line}"); do
	for each in $(eval echo \"${line}\"); do
		echo $each
	done


done < <(sed test.sh -e '/^function includes/,/^}/p;d' | sed '1d;$d')
